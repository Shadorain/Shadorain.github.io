"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const common_tags_1 = require("common-tags");
const path = __importStar(require("path"));
const fs_1 = require("fs");
const dirPath = "./public/.well-known";
const filePath = "brave-rewards-verification.txt";
exports.onPostBuild = async (args, pluginOptions) => {
    const options = pluginOptions;
    if (!pluginOptions) {
        throw new Error("No options passed to gatsby-plugin-verify-brave! Please see usage in package README.");
    }
    if (!pluginOptions.token) {
        throw new Error("No `token` passed to gatsby-plugin-brave! Please see usage in package README.");
    }
    if (!pluginOptions.domain) {
        throw new Error("No `domain` passed to gatsby-plugin-brave! Please see usage in package README.");
    }
    const content = common_tags_1.stripIndent `
    This is a Brave Rewards publisher verification file.

    Domain: ${options.domain}
    Token: ${options.token}

    `;
    await fs_1.promises.mkdir(dirPath, { recursive: true });
    const outputPath = path.join(dirPath, filePath);
    await fs_1.promises.writeFile(outputPath, content);
    args.reporter.success("Brave Rewards verification file created");
};
//# sourceMappingURL=gatsby-node.js.map